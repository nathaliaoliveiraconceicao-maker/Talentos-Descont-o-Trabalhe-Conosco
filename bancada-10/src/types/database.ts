/**
 * Tipos do banco Supabase.
 *
 * Este é um placeholder mínimo. Assim que o projeto Supabase estiver criado
 * e o schema (`supabase/schema.sql`) aplicado, gere os tipos reais com:
 *
 *   npx supabase gen types typescript --project-id <seu-project-id> > src/types/database.ts
 *
 * e este arquivo passa a ser sobrescrito automaticamente por esse comando.
 */
// eslint-disable-next-line
export type Database = any
